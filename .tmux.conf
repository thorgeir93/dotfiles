#Prefix is Ctrl-a
unbind C-b
set -g prefix C-a
bind C-a send-prefix

#Mouse works as expected
set -g mouse on 

# Allowing us to use vim keys to navigate the terminal history.
setw -g mode-keys vi
set -g history-limit 10000

# y and p as in vim
bind Escape copy-mode
unbind p
bind p paste-buffer
bind -t vi-copy 'v' begin-selection
bind -t vi-copy 'y' copy-selection
bind -t vi-copy 'Space' halfpage-down
bind -t vi-copy 'Bspace' halfpage-up

# extra commands for interacting with the ICCCM clipboard
bind C-c run "tmux save-buffer - | xclip -i -sel clipboard"
bind C-v run "tmux set-buffer \"$(xclip -o -sel clipboard)\"; tmux paste-buffer"

# easy-to-remember split pane commands
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# moving between windows with vim movement keys
#bind -r C-h select-window -t :-
#bind -r C-l select-window -t :+

# resize panes with vim movement keys
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

set -g default-terminal "screen-256color"

# Status bar theme
set -g status-bg colour100
set -g status-fg white

# moving between panes with vim movement keys
bind -r k select-pane -U 
bind -r j select-pane -D 
bind -r h select-pane -L 
bind -r l select-pane -R 

# Move between panes with Ctrl + hjkl 
bind -n C-H select-pane -L
bind -n C-L select-pane -R
bind -n C-K select-pane -U
bind -n C-J select-pane -D

# and now unbind keys
unbind Up     
unbind Down   
unbind Left   
unbind Right  

bind e setw synchronize-panes on
bind E setw synchronize-panes off

# Smart pane switching with awareness of Vim splits.
# See: https://github.com/christoomey/vim-tmux-navigator
#is_vim="ps -o state= -o comm= -t '#{pane_tty}' | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|n?vim?x?)(diff)?$'"
#bind-key -n C-h if-shell "$is_vim" "send-keys C-h"  "select-pane -L"
#bind-key -n C-j if-shell "$is_vim" "send-keys C-j"  "select-pane -D"
#bind-key -n C-k if-shell "$is_vim" "send-keys C-k"  "select-pane -U"
#bind-key -n C-l if-shell "$is_vim" "send-keys C-l"  "select-pane -R"
#bind-key -n C-\ if-shell "$is_vim" "send-keys C-\\" "select-pane -l"
bind -n C-k if "[ $(tmux display -p '#{pane_current_command}') = vimx ]" "send-keys C-k" "select-pane -U"
bind -n C-j if "[ $(tmux display -p '#{pane_current_command}') = vimx ]" "send-keys C-j" "select-pane -D"
bind -n C-h if "[ $(tmux display -p '#{pane_current_command}') = vimx ]" "send-keys C-h" "select-pane -L"
bind -n C-l if "[ $(tmux display -p '#{pane_current_command}') = vimx ]" "send-keys C-l" "select-pane -R"
#bind -n C-k if "[ $(tmux display -p '#{pane_current_command}') = vimx -o $(tmux display -p '#{pane_current_command}') = ssh ]" "send-keys C-k" "select-pane -U"
#bind -n C-j if "[ $(tmux display -p '#{pane_current_command}') = vimx -o $(tmux display -p '#{pane_current_command}') = ssh ]" "send-keys C-j" "select-pane -D"
#bind -n C-h if "[ $(tmux display -p '#{pane_current_command}') = vimx -o $(tmux display -p '#{pane_current_command}') = ssh ]" "send-keys C-h" "select-pane -L"
#bind -n C-l if "[ $(tmux display -p '#{pane_current_command}') = vimx -o $(tmux display -p '#{pane_current_command}') = ssh ]" "send-keys C-l" "select-pane -R"
